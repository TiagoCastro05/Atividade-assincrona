<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resultado da Pesquisa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5" style="max-width: 700px;">
        <h2 class="mb-4 text-center">Resultado para "<%= termo %>"</h2>
        <div class="card mb-4">
            <div class="card-body">
                <h4 class="card-title">Clima</h4>
                <% if (weather && weather.weather) { %>
                    <p><strong>Descrição:</strong> <%= weather.weather[0].description %></p>
                    <p><strong>Temperatura:</strong> <%= weather.main.temp %>°C</p>
                    <p><strong>Humidade:</strong> <%= weather.main.humidity %>%</p>
                <% } else { %>
                    <p>Não foi possível obter dados do clima.</p>
                <% } %>
            </div>
        </div>
        <div class="card mb-4">
            <div class="card-body">
                <h4 class="card-title">Resumo Wikipedia</h4>
                <% if (wiki && wiki.extract) { %>
                    <p><%= wiki.extract %></p>
                    <% if (wiki.thumbnail && wiki.thumbnail.source) { %>
                        <img src="<%= wiki.thumbnail.source %>" class="img-fluid mt-2" alt="Imagem Wikipedia">
                    <% } %>
                <% } else { %>
                    <p>Não foi possível obter resumo da Wikipedia.</p>
                <% } %>
            </div>
        </div>
        <div class="text-center">
            <a href="/protected" class="btn btn-secondary">Voltar</a>
        </div>
    </div>
</body>
</html>